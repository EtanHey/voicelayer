# VoiceBar v2.1.17 Ship Pass Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Remove the hover glass-color flicker, collapse the empty teleprompter leading wing, and prepare the exact-head v2.1.17 release candidate.

**Architecture:** Keep `PillHostingView` and `VoiceBarHoverHysteresis` as the only hover-state authority, removing only redundant tracking from the native glass leaf. Express the empty teleprompter side directly in `VoiceBarNotchContract` while leaving the lower body and trailing waveform geometry intact.

**Tech Stack:** Swift 5.9, SwiftUI/AppKit, XCTest, Bun/TypeScript, shell release verification.

---

## Task 1: Lock the regressions RED

**Files:**
- Modify: `flow-bar/Tests/VoiceBarUITests/VoiceBarNotchMaterialTests.swift`
- Modify: `flow-bar/Tests/VoiceBarUITests/VoiceBarNotchContractTests.swift`

1. Replace the native-glass pointer-tracking source assertion with assertions that the glass leaf contains no `NSTrackingArea`, `mouseEntered`, or `mouseExited` path.
2. Change the teleprompter geometry assertions to require `leadingWingWidth == 0` and keep the existing trailing waveform-width assertion.
3. Run `swift test --package-path flow-bar --filter 'VoiceBarNotchMaterialTests|VoiceBarNotchContractTests'` and confirm failures are caused by the current tracking code and nonzero wing width.

## Task 2: Implement the two minimal fixes

**Files:**
- Modify: `flow-bar/Sources/VoiceBarUI/VoiceBarNotchMaterial.swift`
- Modify: `flow-bar/Sources/VoiceBarUI/VoiceBarNotchContract.swift`

1. Delete the glass leaf's private tracking area, `updateTrackingAreas`, `mouseEntered`, and `mouseExited` overrides.
2. Set only the teleprompter geometry's leading wing width to zero.
3. Re-run the focused tests and confirm they pass.

## Task 3: Bump the release metadata

**Files:**
- Modify: `package.json`
- Modify: `server.json`
- Modify: `flow-bar/bundle/Info.plist`

1. Change all release metadata from `2.1.16` to `2.1.17`, including both server registry version fields and both plist version fields.
2. Run metadata-focused tests and a repository search to confirm no active release source remains at `2.1.16`.

## Task 4: Verify the exact candidate

1. Run the full Swift suite with `swift test --package-path flow-bar`.
2. Run the full Bun suite with `bun test` and typecheck if defined by `package.json`.
3. Run the repository's notch corpus and release verification scripts discovered from the current tree.
4. Build/notarize to an isolated destination; launch any QA instance only with offscreen placement and terminate its exact PID.
5. Capture first and repeated hover plus teleprompter pixels, verify no color flash and zero leading wing, and inspect every artifact.
6. Stage `.verified/verified-runtime-fix-notch-363c-ship-v2117b-<short-sha>.txt` with `Verified-Runtime: <full-sha>`.

## Task 5: Publish the worker seam

1. Run bounded local CodeRabbit review and address substantive findings.
2. Commit the scoped changes, push `fix/notch-363c-ship-v2117b`, and open a ready PR against `main`.
3. Put the standalone exact `Verified-Runtime: <head>` line in the PR body.
4. Request `@codex review` and `@cursor @bugbot review`, read responses, and address actionable feedback without merging.
5. Append the PR URL, exact head, verification counts, artifact paths, and explicit lead-owned merge/tag/release seam to `/Users/etanheyman/Gits/orchestrator/collab/2026-07-17-voicelayer-notch-w1-w2.md`.
6. Store the WHAT and WHY in BrainLayer.
